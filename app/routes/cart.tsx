import type { Route } from "./+types/cart";
import { useState } from "react";
import { calculateSubtotal, calculateTax, calculateShipping, calculateTotal } from "~/utils/cartUtils"; // Import the utility functions

const Cart = () => {


  return (
    <div className="p-4">
      <div className="space-y-4">
       
      </div>
    </div>
  );
};

export default Cart;
